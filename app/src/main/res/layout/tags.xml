<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.example.tapiwa.collabo.Main">




    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/unopenedlistView"
        android:numColumns="2"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true">
    </ListView>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:textSize="15dp"
        android:id="@+id/unopenedMessagesBanner"
        android:layout_below="@id/unopenedlistView"
        android:textStyle="bold"
        android:textColor="#000"
        android:gravity="center_horizontal"
        android:text="Opened Tags"
        android:background="#B2B4B5"
        />

    <ListView
        android:layout_width="match_parent"
        android:layout_below="@id/unopenedMessagesBanner"
        android:layout_height="wrap_content"
        android:id="@+id/openedlistView"
        android:numColumns="2">
    </ListView>

</RelativeLayout>
